version: '3'

services:
  web:
    restart: always
    env_file:
      - ./src/.env

  db:
    restart: always
    env_file:
      - ./src/.env

  nginx:
    restart: always
    ports:
      - "80:80"
